# Web Configuration
NODE_ENV=development

# API URL (Local Development)
# Use localhost when running web locally
# Browser will always connect to localhost:3001 (exposed port)
NEXT_PUBLIC_API_URL=http://localhost:3001
